/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: [
      'images.unsplash.com',"i.ibb.co", "img.nbc.com", "ibb.co", "cdn-images.imagevenue.com"
    ]
  }
}
